@effect(io) @extern fun printf(s: str) -> i32;

@effect(io) fun println(s: str) = {
  printf(s + "\n");
}

fun get_message(x: i32) -> str = {
  if x == 0 {
    return "hello, world";
  } else {
    "Hello, world!"
  }
}


@main @effect(io) fun main() = println(get_message(0))
