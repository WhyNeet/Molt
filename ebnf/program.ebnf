program = { statement } ;

statement = declStmt | exprStmt ;

declStmt = { annotation " " } varDecl | funDecl ;
varDecl = "let" IDENTIFIER "=" expression ";" ;

exprStmt = expression ";" ;

annotation = "@" STRING ;
